Widget resultWidget = const LoadingIcon();

FutureBuilder<type>( // TODO poner tipo del dato recibido
  future: endpoint, // TODO poner endpoint que hay que esperar
  builder: (BuildContext context, AsyncSnapshot<type> response) { // TODO poner tipo del dato recibido
    if(response.hasError) {
      resultWidget = Container(
        margin: EdgeInsets.symmetric(
          vertical: MediaQuery.of(context).size.height * 0.05,
        ),
        child: const Text('Error'),
      );
    } else if(response.hasData) {
        resultWidget = Text('RESULT WIDGET AQUÍ'); // TODO poner el widget que se espera ver
    } else {
        resultWidget = Text('NO SE HAN ENCONTRADO DATOS'); // TODO poner widget que explique qué ha pasado
    }
    return resultWidget;
  }
)